// @flow

import type {Border, Color} from '../components/controls'

type ValidateInfo = {
  border?: Border,
  borderColor?: Color,
  backgroundColor?: Color,
  messageColor?: Color,
  icon?: string,
  message?: string
}


export type {
  ValidateInfo
}